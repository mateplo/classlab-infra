# Composant compagnon (kustomize) du chart Helm kube-prometheus-stack.
# Porte les HTTPRoute qui exposent les UIs monitoring via le Gateway `classlab`.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - grafana.yaml
  - prometheus.yaml
  - alertmanager.yaml
